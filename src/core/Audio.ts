import Component from './_Component'; export { Component }
import Instrument from './_Instrument'; export { Instrument }

// API for Audio
// Can use:
//   Component (to build custom Audio Components (ex: Instrument extends this class))
//   Instrument (to build custom Instruments)
export default {
  Component,
  Instrument
}